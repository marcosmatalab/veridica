# Evidencia: recuperación léxica del 3.1 sobre los 100 pares oro

- **Fecha:** 2026-08-13
- **Encargo:** 3.1
- **Commit:** `51eaec1`
- **Corrida:** `corridas_eval` id **2**
- **Configuración:** `tsvector` `spanish`, `websearch_to_tsquery`, `ts_rank_cd`, k=20,
  **siempre con filtro de asignatura**

## El número, partido desde aquí y no solo en el 3.5

| Corte | Global | busqueda | lectura |
|---|---|---|---|
| recall@5 | 36.0 % | **52.6 %** (10/19) | **32.1 %** (26/81) |
| recall@20 | 61.0 % | **73.7 %** (14/19) | **58.0 %** (47/81) |

**Por qué va partido ya.** Los pares `busqueda` se localizaron buscando términos de la pregunta en
el texto, o sea **compartiendo mecanismo con la búsqueda léxica**: es donde esta vía luce mejor por
construcción. Un número único en el 3.1 dejaría ese sesgo cocido antes de que nadie lo viera, y para
cuando el 3.5 lo partiera ya se habrían tomado decisiones sobre el global. **El número honesto para
juzgar la recuperación es el de `lectura`.**

## Qué le hace la configuración `spanish` a los identificadores

Este corpus es medio código y **los cien pares oro están hechos de eso**: `@ModelAttribute`,
`PasswordEncoder`, `BindingResult`. Así que no se hereda como supuesto y no se mide con ejemplos
inventados, sino con los identificadores que aparecen en las preguntas oro.

De los **20** identificadores encontrados, el lematizador español trunca **10**:

| Identificador | Sin lematizar (`simple`) | Lematizado (`spanish`) |
|---|---|---|
| `@ComponentScan` | `componentscan` | `componentsc` |
| `@ModelAttribute` | `modelattribute` | `modelattribut` |
| `@page` | `page` | `pag` |
| `AutoMapper` | `automapper` | `automapp` |
| `CommandLineRunner` | `commandlinerunner` | `commandlinerunn` |
| `HtmlHelper` | `htmlhelper` | `htmlhelp` |
| `PasswordEncoder` | `passwordencoder` | `passwordencod` |
| `TempData` | `tempdata` | `tempdat` |
| `UseStaticFiles` | `usestaticfiles` | `usestaticfil` |
| `ViewData` | `viewdata` | `viewdat` |

**Y aquí está lo que salva a `spanish`, que es lo que había que medir antes de cambiar nada: el
truncado es SIMÉTRICO.** El documento y la consulta pasan por la misma configuración, así que buscar
`ViewData` sigue encontrando `ViewData` aunque las dos se guarden como `viewdat`. Comprobado:
`@ComponentScan` → encuentra, `@ModelAttribute` → encuentra, `@page` → encuentra.

**Lo que sí hace daño, y es otra cosa:** un identificador corto que cae en la misma raíz que una
palabra castellana corriente. Medido: `@page` y **pagar** comparten raíz. Entre identificadores oro no hay
ninguna colisión (0 grupos), así que el ruido no es de unos con otros: es de
identificadores con castellano.

**Decisión, con la evidencia delante: NO se añade hoy una segunda columna `simple`.** Era la salida
obvia y no se toma porque lo medido no la justifica: el emparejamiento aguanta, la colisión afecta a
identificadores cortos y el coste sería otra columna `tsvector`, otro índice GIN por partición y una
lista más que fusionar en el 3.3. Queda escrito como **la primera palanca a tirar si el 3.4 enseña
que los fallos son de terminología exacta**, y con el número que habría que volver a mirar.

## Los que no entran en el top 5

| Par | Grupo | Posición | Pregunta |
|---|---|---|---|
| `oro-001` | busqueda | no aparece | ¿Dónde se almacenan los datos de la sesión de un usuario en Spring Boot, y qué es lo único |
| `oro-002` | busqueda | 8 | ¿Qué anotación se usa en un parámetro de método del controlador para leer un atributo de s |
| `oro-003` | busqueda | no aparece | ¿Para qué tipo de datos son apropiadas las cookies y por qué no se deben guardar en ellas  |
| `oro-005` | busqueda | no aparece | Si el Model se pierde al redirigir, ¿cómo se le pasa un mensaje de éxito a la página de de |
| `oro-009` | busqueda | 6 | ¿Qué directiva de Pebble se usa para insertar un componente reutilizable como el navbar de |
| `oro-011` | busqueda | no aparece | ¿Qué lógica debe ir en el servicio y cuál en el controlador? |
| `oro-014` | busqueda | 18 | ¿Con qué anotación se marca el componente que implementa el acceso a la base de datos o a  |
| `oro-015` | busqueda | 13 | ¿Qué son la inversión de control y la inyección de dependencias en Spring? |
| `oro-017` | busqueda | no aparece | ¿Qué interfaz hay que implementar para ejecutar código justo al iniciarse la aplicación Sp |
| `oro-020` | lectura | no aparece | ¿Cuál es la principal función del componente Controlador en el patrón MVC de ASP.NET Core? |
| `oro-021` | lectura | 18 | ¿Qué componente actúa como el núcleo del pipeline que procesa las peticiones HTTP? |
| `oro-022` | lectura | 14 | ¿Qué tipo de ciclo de vida de un servicio crea una instancia nueva en cada petición HTTP? |
| `oro-023` | lectura | no aparece | ¿Para qué sirve el middleware UseStaticFiles en ASP.NET Core? |
| `oro-024` | lectura | 14 | ¿Qué es y cómo funciona el Model Binding en ASP.NET Core? |
| `oro-025` | lectura | 17 | ¿Qué objetivo tiene el patrón Post-Redirect-Get tras procesar un formulario POST? |

## Cómo se reproduce

```bash
DATABASE_URL=... python scripts/medir_recuperacion.py --evidencia docs/evidencia/2026-08-13-lexica.md
```

No gasta dinero: es SQL contra la base local. Antes de correrlo, `python scripts/verificar_oro.py`,
que es la regla del 3.0 —un par oro desplazado no da error, da ruido con aspecto de dato—.
