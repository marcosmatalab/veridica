# El corpus JAMAS entra en el contexto de construccion: son ~390 MB que no pinta nada
# dentro de ninguna imagen (la ingesta corre en Windows, con la GPU).
corpus/

.git/
.github/
docs/
evals/
scripts/
tests/
# web/ estaba aqui desde el 0.3, cuando era una carpeta vacia. En el 2.4 es la interfaz que sirve
# la propia API, asi que tiene que entrar en la imagen: son cinco ficheros estaticos, sin build.
deploy/
*.md
.env
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
